# skillpm 更新日志

版本号语义：主版本＝命令或配置不兼容；次版本＝加功能；修订号＝修 bug，行为不变。
改了 `src/skillpm/__init__.py` 里的版本号就要在这写一条，`tools/check_release.py` 会检查。

## 2.2.0 — 2026-09-23

**仓库里的 Skill 可以设成要口令才能装；几个 Skill 可以设成互斥（同一宿主只装一个）。**

起因：同一个工具分了只读 / 读写 / 管理三个版本，可 `install` 的列表默认全选，一回车三个全装上，分级起不到管控作用。
哪个 Skill 要口令、用哪个口令组、哪些互斥，都由仓库根目录的 `skillpm.repo.json` 决定，不写死在工具里。

- **要口令的 Skill 加密存放**：仓库里只放 `sealed/<名字>.pkg`，装的时候在终端里输口令解开。同一口令组共用一个口令。
  口令只能手敲（不显示、不进命令行）；没有终端（AI 替人跑）时直接停下，让人自己去终端运行。输错 3 次不装，原来装的不动。
  输对一次记在本机 `~/.skillpm/keys.json`（600），同组别的 Skill 和以后的 `update` 不再问；口令被更换时 `update` 跳过这一项、旧版留着，提示重新输。
- **默认不装要口令的**：交互列表、`--all`、按仓库装、「仓库里有没装」的提示都不带它们，只能点名装。
- **互斥组**：同一宿主只留一个，装高的自动换掉低的，降回去不用口令；以前一回车全装上的，第一次 `update` 输口令选保留哪个，直接回车只留不要口令的那个。
- `status` 显示「权限：write」或「口令组：xxx」。
- 新命令 `skillpm publish`（维护仓库的人用）：配置里要口令的加密进 `sealed/`，其余复制到 `skills/`，重建 manifest。
  已有这个组的包时先核对口令，输错不发；内容没变的不重做；`--new-password` 换口令时组里的 Skill 要一起发。
- 加密只用标准库：PBKDF2-HMAC-SHA256 派生密钥，HMAC-SHA256 生成密钥流加密并校验，口令不对或包被改过都解不开。
  内容用随机密钥加密、随机密钥再用口令加密（信封加密），以后要「每人一个口令、单独撤销某个人」不用改包格式。
- 为什么要加密而不是只判断口令：工具装在各人机器上，改一行判断就绕过了；仓库人人能拉，明文目录拷走就能用。
  拿到口令的人能把解开的文件再转给别人，这个管不住；最终兜底仍是平台账号本身的权限。

## 2.1.3 — 2026-09-23

- `self-update` 最后那句「使用手册也更新了」也改成给绝对地址（加粗正红），复制到浏览器地址栏就能打开，
  不再只说一句 `skillpm docs`。和 `-h` 底部一致。

## 2.1.2 — 2026-09-23

- 手册地址里的空格换成 `%20`：Windows 用户名常带空格（`C:\Users\Zhang San\…`），地址里带空格，浏览器可能当成搜索词。
  macOS 给 `file:///Users/…`，Windows 给 `file:///C:/Users/…`，两边都是复制到浏览器地址栏就能打开。

## 2.1.1 — 2026-09-23

- `skillpm -h` 底部改成给手册的**绝对地址**（`file:///Users/你/.skillpm-src/docs/使用手册.html`），复制粘到浏览器地址栏就能打开。
  以前给的是 `skillpm docs` 和带 `~` 的 `open` 命令，都没法直接粘进浏览器。带 `file://` 前缀是因为 Safari 不认光秃秃的路径。

## 2.1.0 — 2026-09-23

- **没装的 Skill 会提示出来**：以前 `update` 只管装过的，仓库新加了 Skill 一声不吭——别的 Skill 已经开始依赖它了，本机却根本没装。
  现在 `update` 和 `status` 每次都列出「仓库里有、本机还没装」的，给出 `skillpm install <名字>`，直到装上为止。
  - 只提示，不自动装：按权限分级、只装一个级别的 Skill（SKILL.md 里写了 `tier`，比如 xx-read / xx-write / xx-admin），
    自动补上就越权了；同一个 Skill 的其它级别也不算「没装」。
  - 只看从这个仓库装过东西的宿主；配了仓库却一个都没装过的，当你是有意不用。
- **新命令 `skillpm ignore <名字>`**：有的 Skill 就是不打算装，忽略它就不再提示。
  `skillpm ignore` 列出忽略了哪些，`skillpm ignore --undo <名字>` 恢复提示。
- **配色换成固定色号，浅色终端上不再发白发虚**：以前用的 91~96「亮色」由终端主题决定颜色，
  macOS 终端默认白底主题下亮青、亮绿很淡。现在用 256 色里中等亮度的固定色号，白底黑底都读得清；
  命令和代码单独用紫色，灰色加深一档，要照着敲的命令（「要装：…」、`-h` 底部的手册入口）用加粗正红。
- 更新日志在终端里更好读：版本标题上色加粗，列表的 `-` 换成彩色圆点。

## 2.0.7 — 2026-09-21

- **手册顶部的三篇切换更显眼**：以前「快速上手 / 完整命令手册 / 接入自己的仓库」看着像普通文字，容易以为手册只有一页。
  现在做成一组带边框的按钮，每篇带编号，当前篇蓝底白字，旁边写着「手册共 3 篇 →」。
- 每篇末尾加了「下一篇」卡片，读完点一下就翻到下一篇。

## 2.0.6 — 2026-09-21

- **修 Windows 下 `skillpm -h` 底部乱码**：老式 cmd 不认识超链接转义（OSC 8），会原样打成 `]8;;file:///…` 一串乱码。
  超链接转义整个去掉，只输出纯文字——反正常用终端也点不开。
- Windows 下打开手册的命令从 `start "" "…"` 改成 `explorer "…"`：`start` 在 PowerShell 里会报错，`explorer` 在 cmd 和 PowerShell 都能用。
- Windows 下 `-h` 最后一行显示配置目录的真实路径（`C:\Users\你\.skillpm\`），不再写 Windows 上没意义的 `~/.skillpm/`。

## 2.0.5 — 2026-09-21

- **手册改成「复制一条命令就能打开」**：macOS 自带的「终端」点不开 `file://` 链接（⌘ 单击、双击、右键都试过），
  所以 `skillpm -h` 底部改成给一条现成的命令——macOS `open ~/.skillpm-src/docs/使用手册.html`、Windows `start`、Linux `xdg-open`，
  复制粘贴回车就在浏览器里打开。支持超链接的终端（iTerm2、VS Code、Windows Terminal……）里这条命令本身也能 ⌘/Ctrl 点开。
  最省事的还是直接敲 `skillpm docs`。
- 手册文件名改回 `docs/使用手册.html`：上一版为了让终端认出整条链接改成了英文，现在不走链接了，不需要了。

## 2.0.4 — 2026-09-21

- **终端里点一下就能打开手册**：`skillpm -h` 底部和 `skillpm docs` 都给出 `file://…/manual.html` 链接，
  按住 ⌘（Windows 按 Ctrl）点它就在浏览器里打开。支持超链接的终端（iTerm2、VS Code、Windows Terminal……）还会带上可点的超链接转义；
  接管道、写文件时只输出文字。
- 手册文件改名 `docs/使用手册.html` → `docs/manual.html`：文件名全英文，终端才认得出整条链接。
- 手册开头的黄框：源码地址一行一个，加了一句邀请大家给 GitHub 仓库点 Star。

## 2.0.3 — 2026-09-21

- `skillpm -h` 底部写明图文手册是 HTML、文件在哪（`~/.skillpm-src/docs/使用手册.html`）。以前只写「图文使用手册：skillpm docs」，
  容易以为是另一份文档。

## 2.0.2 — 2026-09-21

- **修：Windows 卸载脚本明明卸好了，却返回失败。** 脚本里检查「是不是 pip 装的」那一步，没用 pip 装时会返回 1，
  被 PowerShell 当成整个脚本的退出码；在自己的脚本里调用卸载会误以为失败。现在正常走完就返回 0。
- 测试：读子进程输出时按 UTF-8 解码（Windows 默认用系统代码页，读中文会乱）。

## 2.0.1 — 2026-09-21

**修：Windows 上输出被重定向时，一打中文就崩。** 英文 Windows 的系统代码页是 cp1252，编不了中文；
输出接管道、写文件或在 CI 里跑时，Python 就用它编码，直接 `UnicodeEncodeError`。现在启动时把输出流改成 UTF-8。
中文 Windows 的代码页恰好能编中文，所以之前没暴露——是开源后第一次跑 Windows CI 抓到的。

- `~` 统一用同一个家目录来源展开：Windows 上 Agent 目录表和自动发现以前可能看的是两个不同的家目录。
- CI：在 Mac / Linux / Windows 上真的跑一遍安装脚本和卸载脚本。

## 2.0.0 — 2026-09-21

**首个公开版本。** 把放在 git 仓库里的 AI Skill 一键装到本机的各个 Agent，并跟着仓库更新。

**和 1.x 的不兼容之处：不再内置默认的 Skill 仓库。** 第一次运行 `skillpm install` 时必须填你们团队的仓库地址；
已经配好的人不受影响。

### 能做什么

- **一键装到多个 Agent**：认识 52 种常见 Agent 的 Skill 目录（Claude Code、Codex、Cursor、Windsurf、Gemini CLI、
  Qoder、Trae、WorkBuddy……），表里没有的按「家目录的隐藏文件夹下有 skills」自动发现，也能手填目录。
- **跟着仓库更新**：`skillpm update` 列出新版本，确认后更新，并把每个 Skill 的更新日志念给你；旧版自动备份。
- **不碰别人的东西**：本地改过的、不是本工具装的 Skill，安装和更新都跳过并说明；`--force` 覆盖前先完整备份。
- **多个仓库**：可以同时配多个仓库。同名 Skill 换来源只在你自己 `install` 时发生、而且会先问你；
  `update` 永远只跟着装的时候那个仓库。
- **Skill 仓库几乎零要求**：通用 Agent Skill 格式（`skills/<名字>/SKILL.md`，写好 `name`、`description`）就能装；
  `version`、`CHANGELOG.md`、`manifest.json` 都可选。`skillpm manifest --check` 自检，适合放 CI。
- **私有仓库**：`--token` 访问令牌（不进命令行、不落盘到缓存的 git 配置），或者 SSH。
- **锁版本**：`skillpm freeze` 导出锁文件，别人 `install --from` 装到完全一样的一套。
- **项目级安装**：`-p` 只装到当前项目，版本写进锁文件随项目提交。
- **中文友好**：全部提示和报错是中文，写错命令会猜你想写什么并列出常用写法；拉取失败按原因给出下一步。
- **图文手册随工具发**：`skillpm docs` 在浏览器里打开，快速上手每一步都有真实截图。
- **零依赖**：只用 Python 3.8+ 标准库；Mac / Windows / Linux 通用，安装脚本默认不碰 PyPI。
